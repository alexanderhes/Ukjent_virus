/*
 * EsViritu Nextflow Pipeline — main configuration
 */

// Load default parameters
includeConfig 'conf/params.config'

// Docker — always enabled (matches deployment environment)
docker {
    enabled    = true
    runOptions = '-u $(id -u):$(id -g)'
}

// Container per process
includeConfig 'conf/docker.config'

// Resource labels
includeConfig 'conf/base.config'

// Executor
executor {
    name      = 'local'
    queueSize = 8
}

// Capture Nextflow log files into the output directory
timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/timeline.html"
    overwrite = true
}
report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/report.html"
    overwrite = true
}
trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/trace.txt"
    overwrite = true
}

// ── Profiles ────────────────────────────────────────────────────────────────
// Activate with: -profile <name> or combined: -profile server,db_v3_2_4
//
//  server        — host/infrastructure paths for the sequencing server
//  db_<alias>    — EsViritu database selection; one profile per DB version
//                  Add a new block here to register a new database alias.
profiles {

    // ── Host environment ───────────────────────────────────────────────────
    server {
        params {
            // Bowtie2 index prefix — Nextflow globs host_index*.bt2 from this path
            host_index = '/mnt/tempdata/ukjent_virus_dbs/host_ref/host_index'
        }
    }

    // ── EsViritu database versions ─────────────────────────────────────────
    // Each profile sets params.esviritu_db to the corresponding directory.
    // Pass -d <alias> to NGS_wrapper.sh to select one at runtime.

    db_v3_2_4 {
        params {
            esviritu_db = '/mnt/tempdata/ukjent_virus_dbs/db/esviritu_DB/v3.2.4'
        }
    }
    db_HEV {
        params {
            esviritu_db = '/mnt/tempdata/ukjent_virus_dbs/db/HEV_595/'
        }
    }
}
